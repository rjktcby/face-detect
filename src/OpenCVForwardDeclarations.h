#ifndef __FACE_DETECT_OPENCV_FORWARD_DECLS_H__
#define __FACE_DETECT_OPENCV_FORWARD_DECLS_H__

// Here we declare all necessary OpenCV types, so that
// we need to import C++ headers only in .mm files

typedef struct _IplImage IplImage;
struct CvMemStorage;
struct CvCapture;

#endif // __FACE_DETECT_OPENCV_FORWARD_DECLS_H__